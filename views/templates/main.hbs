<!doctype html>

<html lang="en" class="{{htmlClass}}">
    {{> head}}
    <body class="{{bodyClass}}">
        {{> header}}
        <main id="content-wrapper" role="main">
            {{{body}}}
        </main>
        {{> footer}}
        {{> scripts}}
        {{> ga}}
    </body>    
</html>